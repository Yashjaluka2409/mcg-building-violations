# MCG Building Violation Management System - backend (FastAPI + async SQLAlchemy 2.0 + Alembic + Pydantic v2)
# Same stack as the MCG platform backend (sanitary-monitoring-system-be). Python 3.11+ (tested on 3.12 - 3.14).
fastapi>=0.116
uvicorn[standard]>=0.30
gunicorn>=23
sqlalchemy[asyncio]>=2.0.43
asyncpg>=0.29              # PostgreSQL (production)
aiosqlite>=0.20            # SQLite (demo / tests)
greenlet>=3
alembic>=1.14
pydantic>=2.11
pydantic-settings>=2.10
python-multipart>=0.0.20
python-dotenv>=1.0
pyjwt>=2.9
apscheduler>=3.10,<4
jinja2>=3.1
httpx>=0.27
requests>=2.32
boto3>=1.35               # AWS S3 file storage (ap-south-1); local /uploads fallback when unset
cryptography>=43
cbor2>=5.6                # Apple App Attest objects (services/attestation.py)
Pillow>=11.1
qrcode[pil]>=8.0
reportlab>=4.2
pyHanko[pkcs11,image-support]>=0.25
pyhanko-certvalidator>=0.26
shapely>=2.0.6
pyshp>=2.3
openpyxl>=3.1
# Bilingual (Hindi) PDF rendering with proper Devanagari shaping. Needs Pango on the host:
#   Ubuntu/Debian: apt install libpango-1.0-0 libpangoft2-1.0-0 ; macOS: brew install pango
weasyprint>=63
# tests
pytest>=8
anyio>=4
